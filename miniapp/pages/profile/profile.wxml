<view class="page">
  <view class="profile-card" bindtap="goEdit">
    <image class="avatar" src="{{profile.member.avatar || '/assets/default-avatar.png'}}" mode="aspectFill"/>
    <view class="info">
      <text class="name">{{profile.member && profile.member.name || (profile.role && profile.role) || '未登录'}}</text>
      <text class="role">{{profile.member && profile.member.org_position || roleLabel(profile.role) || ''}}</text>
      <text wx:if="{{profile.branch && profile.branch.name}}" class="branch">{{profile.branch.name}}</text>
    </view>
    <view class="badge" wx:if="{{unreadCount > 0}}">{{unreadCount}}</view>
  </view>

  <!-- 未绑定手机号时显示 -->
  <view class="bind-phone-bar" wx:if="{{!profile.member}}">
    <button class="btn-bind-phone" open-type="getPhoneNumber" bindgetphonenumber="onGetPhone">📱 绑定手机号</button>
    <text class="bind-tip">绑定后自动关联会员档案</text>
  </view>

  <view class="menu-section">
    <view class="menu-item" bindtap="goEdit">
      <text class="icon">✏️</text>
      <text class="label">编辑资料</text>
      <text class="arrow">→</text>
    </view>
    <view class="menu-item" bindtap="goMyEvents">
      <text class="icon">📅</text>
      <text class="label">我的活动</text>
      <text class="arrow">→</text>
    </view>
    <view class="menu-item" bindtap="goMyInfos">
      <text class="icon">📝</text>
      <text class="label">我的社情民意</text>
      <text class="arrow">→</text>
    </view>
    <view class="menu-item" bindtap="goMyScores">
      <text class="icon">⭐</text>
      <text class="label">我的积分</text>
      <text class="arrow">→</text>
    </view>
    <view class="menu-item" bindtap="goMessages">
      <text class="icon">💬</text>
      <text class="label">消息中心</text>
      <view class="right">
        <view class="msg-badge" wx:if="{{unreadCount > 0}}">{{unreadCount}}</view>
        <text class="arrow">→</text>
      </view>
    </view>
  </view>

  <view class="logout-section" wx:if="{{profile.role}}">
    <button class="btn-logout" bindtap="logout">退出登录</button>
  </view>
</view>